<!DOCTYPE html>
<!-- ===== Page: Forgot Password ===== -->
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>ลืมรหัสผ่าน - Home4Pets</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" href="/style.css">
</head>
<body class="auth-page" style="background-color: #7fd1c2;">

<!-- Auth Container -->
<div class="auth-container">
  <div class="auth-card">
    <!-- Logo with House Icon -->
    <div class="auth-logo">
      <img src="/green_house_logo-removebg-preview.png" alt="Logo" style="height: 30px; vertical-align: middle; margin-right: 8px;"><span>Home4Pets</span>
    </div>

    <!-- Title -->
    <h3 class="auth-title">ลืมรหัสผ่าน</h3>

    <!-- Error Message -->
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <!-- Success Message -->
    <% if (typeof success !== 'undefined' && success) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <!-- Form -->
    <form method="POST" action="/forgot-password">
      <div class="form-group">
        <input type="email" name="email" class="auth-input" placeholder="📧 Email" required>
      </div>

      <div class="form-group">
        <input type="password" name="newPassword" class="auth-input" placeholder="🔒 รหัสผ่านใหม่" required>
      </div>

      <div class="form-group">
        <input type="password" name="confirmPassword" class="auth-input" placeholder="🔒 ยืนยันรหัสผ่านใหม่" required>
      </div>

      <button type="submit" class="auth-btn">เปลี่ยนรหัสผ่าน</button>
    </form>

    <!-- Back Link -->
    <p class="auth-footer">
      กลับไปที่ <a href="/login">เข้าสู่ระบบ</a>
    </p>
  </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
