<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>รายละเอียดสัตว์ - Home4Pets</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<!-- Navbar -->
<nav class="navbar px-4">
  <div class="d-flex align-items-center gap-2">
    <img src="/green_house_logo-removebg-preview.png" style="height:30px;">
    <a href="/" class="navbar-brand text-decoration-none">Home4Pets</a>
  </div>

  <div class="d-flex align-items-center gap-3">
    <a class="nav-link" href="/home">หน้าหลัก</a>
    <a class="nav-link" href="/recommended">รายการแนะนำ</a>
    <a class="nav-link" href="/favorites">รายการโปรด</a>

    <div class="user-badge">
      <span><%= user.name %></span>
      <img src="https://cdn-icons-png.flaticon.com/512/847/847969.png">
    </div>
  </div>
</nav>

<!-- Banner -->
<section class="hero" style="background-image:url('/banner.jpg')"></section>

<!-- Content -->
<div class="container mt-5">
  <div class="row">

    <!-- รูปแมว -->
    <div class="col-md-5">
      <img
        src="<%= petData.name === 'ปลาเผา' ? '/conun.jpg' : '/cat.jpg' %>"
        alt="<%= petData.name %>"
        class="w-100 pet-img">

      <div class="mt-3">
        <span onclick="toggleFavorite(this)" class="heart">🤍</span>
      </div>
    </div>

    <!-- ข้อมูลแมว -->
    <div class="col-md-7">
      <h2><%= petData.name %></h2>

      <div class="pet-box">
        <p class="pet-title">
          สวัสดีครับ , ผมชื่อ <%= petData.name %> ครับ
        </p>

        <p><strong>รหัสแมว :</strong> <%= petData.id %></p>
        <p><strong>เพศ :</strong> <%= petData.gender %></p>
        <p><strong>อายุ :</strong> <%= petData.age %></p>
        <p><strong>สถานะสุขภาพ :</strong> ทำหมันแล้ว</p>

        <!-- ✅ แก้เฉพาะสี -->
        <p>
          <strong>สี :</strong>
          <%= petData.name === 'โคนัน' ? 'ขาว' : 'ขาว-น้ำตาล' %>
        </p>

        <p class="mt-3">ข้อมูลสัตว์เลี้ยง</p>
      </div>

      <a href="/home" class="btn btn-secondary mt-3">← กลับ</a>
    </div>

  </div>
</div>

<footer class="text-center text-muted mt-5 mb-3">
  © 2026 Home4Pets | Project for Education
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
function toggleFavorite(el) {
  const petId = '<%= petData.id %>';
  let fav = JSON.parse(localStorage.getItem('favorites') || '[]');

  if (fav.includes(petId)) {
    fav = fav.filter(id => id !== petId);
    el.textContent = '🤍';
  } else {
    fav.push(petId);
    el.textContent = '❤️';
  }

  localStorage.setItem('favorites', JSON.stringify(fav));
}
</script>

</body>
</html>
