<!DOCTYPE html>
<!-- ===== Page: Pet Detail ===== -->
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>รายละเอียดสัตว์ - Home4Pets</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" href="/style.css">
</head>
<body class="minimal-theme">

<!-- Navbar -->
<nav class="navbar px-4">
  <div class="d-flex align-items-center gap-2">
    <img src="/green_house_logo-removebg-preview.png" style="height:30px;">
    <a href="/" class="navbar-brand text-decoration-none">Home4Pets</a>
  </div>

  <div class="d-flex align-items-center gap-3">
    <a class="nav-link" href="/home">หน้าหลัก</a>
    <a class="nav-link" href="/home">ค้นหาสัตว์</a>
    <a class="nav-link" href="/recommended">รายการแนะนำ</a>
    <a class="nav-link" href="/favorites">รายการโปรด</a>

    <div class="dropdown user-menu">
      <button class="user-badge user-menu-toggle dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
        <span><%= user.name %></span>
        <img src="https://cdn-icons-png.flaticon.com/512/847/847969.png">
      </button>
      <ul class="dropdown-menu dropdown-menu-end">
        <li><a class="dropdown-item" href="/profile/edit">แก้ไขข้อมูล</a></li>
        <li><a class="dropdown-item" href="/profile/password">เปลี่ยนรหัสผ่าน</a></li>
        <li><hr class="dropdown-divider"></li>
        <li><a class="dropdown-item text-danger" href="/logout">ออกจากระบบ</a></li>
      </ul>
    </div>
  </div>
</nav>

<div class="notification-float">
  <a href="/notifications" class="notification-bell" id="bellButton" aria-label="Notifications">
    🔔
    <span class="notification-count" id="notificationCount">0</span>
  </a>
  <div class="notification-panel" id="notificationPanel">
    <div class="notification-empty">ยังไม่มีการแจ้งเตือน</div>
  </div>
</div>

<!-- Banner -->
<section class="hero" style="background-image:url('/banner.jpg')"></section>

<!-- Content -->
<div class="container mt-5">
  <div class="pet-detail-card minimal-detail">
    <div class="pet-detail-left">
      <div id="petCarousel" class="carousel slide pet-carousel minimal-carousel" data-bs-ride="carousel">
        <div class="carousel-inner">
          <div class="carousel-item active">
            <img src="<%= petData.image %>" class="d-block w-100 pet-img" alt="<%= petData.name %>">
          </div>
          <% if (petData.image2) { %>
          <div class="carousel-item">
            <img src="<%= petData.image2 %>" class="d-block w-100 pet-img" alt="<%= petData.name %>">
          </div>
          <% } %>
          <% if (petData.image3) { %>
          <div class="carousel-item">
            <img src="<%= petData.image3 %>" class="d-block w-100 pet-img" alt="<%= petData.name %>">
          </div>
          <% } %>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#petCarousel" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#petCarousel" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>
      <div class="pet-detail-fav">
        <button type="button" onclick="toggleFavorite(this)" class="heart-btn minimal-heart" id="petHeart" aria-label="Favorite">🤍</button>
      </div>

    </div>

    <div class="pet-detail-right">
      <div class="detail-header">
        <p class="pet-title">
          สวัสดีครับ, ผมชื่อ <%= petData.name %> ครับ
        </p>
        <div class="detail-tags">
          <span class="detail-tag">รหัส <%= petData.petId || '-' %></span>
          <span class="detail-tag"><%= petData.type === 'cats' ? 'แมว' : 'สุนัข' %></span>
          <span class="detail-tag"><%= petData.gender === 'female' || petData.gender === '♀' ? '♀ เพศเมีย' : '♂ เพศผู้' %></span>
        </div>
      </div>

      <div class="detail-stats minimal-stats">
        <div class="detail-stat">
          <span class="detail-stat-label">สี</span>
          <span class="detail-stat-value"><%= petData.color || '-' %></span>
        </div>
        <div class="detail-stat">
          <span class="detail-stat-label">อายุ</span>
          <span class="detail-stat-value">🎂 <%= petData.age %></span>
        </div>
        <div class="detail-stat">
          <span class="detail-stat-label">วัคซีน</span>
          <span class="detail-stat-value"><%= (petData.vaccinated || 'no').toLowerCase() === 'yes' ? 'ฉีดแล้ว' : 'ยังไม่ฉีด' %></span>
        </div>
      </div>

      <div class="detail-status minimal-status">
        <div class="detail-status-item">
          <div class="pet-detail-section-title">สถานะสุขภาพ</div>
          <div class="pet-detail-pill"><%= petData.sterilized === 'yes' ? 'ทำหมันแล้ว' : 'ยังไม่ทำหมัน' %></div>
        </div>

        <div class="detail-status-item">
          <div class="pet-detail-section-title">สถานะการรับเลี้ยง</div>
          <div class="pet-detail-pill"><%= petData.adoptionStatus === 'adopted' ? 'ได้รับการรับเลี้ยงแล้ว' : 'พร้อมรับเลี้ยง' %></div>
        </div>
      </div>

      <div class="pet-detail-section minimal-section">
        <div class="pet-detail-section-title">ข้อมูลสัตว์เลี้ยง</div>
        <div class="pet-detail-desc"><%= petData.details || '-' %></div>
      </div>

      <div class="pet-detail-section minimal-section">
        <div class="pet-detail-section-title">ช่องทางติดต่อ</div>
        <div class="contact-chips">
          <a class="contact-chip" href="tel:+66999999999">📞 099-999-9999</a>
          <a class="contact-chip" href="https://line.me/ti/p/~home4pets" target="_blank" rel="noopener">💬 LINE: @home4pets</a>
        </div>
      </div>

      <a href="/home" class="btn btn-back mt-3">← กลับ</a>
    </div>
  </div>
</div>

<footer class="text-center text-muted mt-5 mb-3">
  © 2026 Home4Pets | Project for Education
</footer>

<script type="application/json" id="favoriteIdsData"><%- JSON.stringify(favoriteIds || []) %></script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
async function toggleFavorite(el) {
  const petId = '<%= petData.id %>';
  const body = new URLSearchParams({ petId });
  const response = await fetch('/favorites/toggle', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body
  });
  const data = await response.json();
  if (data && data.success) {
    el.textContent = data.favorited ? '❤️' : '🤍';
  }
}

window.addEventListener('load', function() {
  const favorites = JSON.parse(document.getElementById('favoriteIdsData')?.textContent || '[]');
  const petId = '<%= petData.id %>';
  const heart = document.getElementById('petHeart');
  if (heart && favorites.includes(petId)) {
    heart.textContent = '❤️';
  }
});
</script>

<script>
function formatNotificationType(type) {
  if (type === 'favorite_adopted') return 'สัตว์ในรายการโปรดถูกรับเลี้ยง';
  if (type === 'new_pet_match') return 'มีสัตว์ใหม่ตรงกับความสนใจ';
  return 'การแจ้งเตือน';
}

async function loadNotifications(panelEl) {
  try {
    const response = await fetch('/notifications/latest');
    const data = await response.json();
    if (!data || !data.success) return;

    const items = data.notifications || [];
    if (items.length === 0) {
      panelEl.innerHTML = '<div class="notification-empty">ยังไม่มีการแจ้งเตือน</div>';
      return;
    }
    panelEl.innerHTML = items.map(item => `
      <a class="notification-item ${item.isRead ? '' : 'notification-unread'}" href="/pet/${item.petId}?notificationId=${item.id}">
        <div>${item.message}</div>
        <small class="text-muted">${formatNotificationType(item.type)} • ${new Date(item.createdAt).toLocaleString('th-TH')}</small>
      </a>
    `).join('');
  } catch (err) {
    // ignore
  }
}

(function initNotifications() {
  const countEl = document.getElementById('notificationCount');
  const panelEl = document.getElementById('notificationPanel');
  const bellButton = document.getElementById('bellButton');
  if (!countEl) return;

  if (bellButton && panelEl) {
    bellButton.addEventListener('click', (event) => {
      event.preventDefault();
      panelEl.classList.toggle('show');
      if (panelEl.classList.contains('show')) {
        loadNotifications(panelEl);
      }
    });
    document.addEventListener('click', (event) => {
      if (!panelEl.contains(event.target) && !bellButton.contains(event.target)) {
        panelEl.classList.remove('show');
      }
    });
  }

  const source = new EventSource('/notifications/stream');
  source.addEventListener('message', (event) => {
    try {
      const data = JSON.parse(event.data || '{}');
      const count = Number(data.unreadCount || 0);
      countEl.textContent = count > 99 ? '99+' : String(count);
      countEl.style.display = count > 0 ? 'inline-flex' : 'none';
    } catch (err) {
      // ignore
    }
  });
})();
</script>

</body>
</html>
