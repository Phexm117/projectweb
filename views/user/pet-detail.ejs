<!DOCTYPE html>
<!-- ===== Page: Pet Detail ===== -->
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>รายละเอียดสัตว์ - Home4Pets</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<!-- Navbar -->
<nav class="navbar px-4">
  <div class="d-flex align-items-center gap-2">
    <img src="/green_house_logo-removebg-preview.png" style="height:30px;">
    <a href="/" class="navbar-brand text-decoration-none">Home4Pets</a>
  </div>

  <div class="d-flex align-items-center gap-3">
    <a class="nav-link" href="/home">หน้าหลัก</a>
    <a class="nav-link" href="/home">ค้นหาสัตว์</a>
    <a class="nav-link" href="/recommended">รายการแนะนำ</a>
    <a class="nav-link" href="/favorites">รายการโปรด</a>

    <div class="user-badge">
      <span><%= user.name %></span>
      <img src="https://cdn-icons-png.flaticon.com/512/847/847969.png">
    </div>
  </div>
</nav>

<!-- Banner -->
<section class="hero" style="background-image:url('/banner.jpg')"></section>

<!-- Content -->
<div class="container mt-5">
  <div class="pet-detail-card">
    <div class="pet-detail-left">
      <div id="petCarousel" class="carousel slide pet-carousel" data-bs-ride="carousel">
        <div class="carousel-inner">
          <div class="carousel-item active">
            <img src="<%= petData.image %>" class="d-block w-100 pet-img" alt="<%= petData.name %>">
          </div>
          <% if (petData.image2) { %>
          <div class="carousel-item">
            <img src="<%= petData.image2 %>" class="d-block w-100 pet-img" alt="<%= petData.name %>">
          </div>
          <% } %>
          <% if (petData.image3) { %>
          <div class="carousel-item">
            <img src="<%= petData.image3 %>" class="d-block w-100 pet-img" alt="<%= petData.name %>">
          </div>
          <% } %>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#petCarousel" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#petCarousel" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>
      <div class="pet-detail-fav">
        <span onclick="toggleFavorite(this)" class="heart">🤍</span>
      </div>
    </div>

    <div class="pet-detail-right">
      <p class="pet-title" style="font-size: 26px; font-weight: 700;">
        สวัสดีครับ, ผมชื่อ <%= petData.name %> ครับ
      </p>

      <div class="pet-detail-grid">
        <div class="pet-info-item">
          <div class="pet-info-label">เพศ</div>
          <div class="pet-info-box"> <%= (petData.gender === 'female' || petData.gender === '♀') ? '♀ เพศเมีย' : '♂ เพศผู้' %></div>
        </div>
        <div class="pet-info-item">
          <div class="pet-info-label">ประเภท</div>
          <div class="pet-info-box"><%= petData.type === 'cats' ? 'แมว' : 'สุนัข' %></div>
        </div>
        <div class="pet-info-item">
          <div class="pet-info-label">สี</div>
          <div class="pet-info-box"><%= petData.color || '-' %></div>
        </div>
        <div class="pet-info-item">
          <div class="pet-info-label">อายุ</div>
          <div class="pet-info-box">🎂 <%= petData.age %></div>
        </div>
        <div class="pet-info-item">
          <div class="pet-info-label">รหัสสัตว์</div>
          <div class="pet-info-box"><%= petData.petId || '-' %></div>
        </div>
      </div>

      <div class="pet-detail-row">
        <div class="pet-detail-section">
          <div class="pet-detail-section-title">สถานะสุขภาพ</div>
          <div class="pet-detail-pill"><%= petData.sterilized === 'yes' ? 'ทำหมันแล้ว' : 'ยังไม่ทำหมัน' %></div>
        </div>

        <div class="pet-detail-section">
          <div class="pet-detail-section-title">สถานะการรับเลี้ยง</div>
          <div class="pet-detail-pill"><%= petData.adoptionStatus === 'adopted' ? 'ได้รับการรับเลี้ยงแล้ว' : 'พร้อมรับเลี้ยง' %></div>
        </div>
      </div>

      <div class="pet-detail-section">
        <div class="pet-detail-section-title">ข้อมูลสัตว์เลี้ยง</div>
        <div class="pet-detail-desc"><%= petData.details || '-' %></div>
      </div>

      <a href="/home" class="btn btn-back mt-3">← กลับ</a>
    </div>
  </div>
</div>

<footer class="text-center text-muted mt-5 mb-3">
  © 2026 Home4Pets | Project for Education
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
function toggleFavorite(el) {
  const petId = '<%= petData.id %>';
  const userKey = 'favorites_user_<%= user.id %>';
  let fav = JSON.parse(localStorage.getItem(userKey) || '[]');

  if (fav.includes(petId)) {
    fav = fav.filter(id => id !== petId);
    el.textContent = '🤍';
  } else {
    fav.push(petId);
    el.textContent = '❤️';
  }

  localStorage.setItem(userKey, JSON.stringify(fav));
}
</script>

</body>
</html>
