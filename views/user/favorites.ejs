<!DOCTYPE html>
<!-- ===== Page: Favorites ===== -->
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>รายการโปรด - Home4Pets</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<nav class="navbar">
  <div class="d-flex align-items-center gap-2">
    <img src="/green_house_logo-removebg-preview.png" alt="Logo" style="height: 30px; margin-right: 8px;"> <a href="/home" style="text-decoration: none;"><span class="navbar-brand">Home4Pets</span></a>
  </div>

  <div class="d-flex align-items-center">
    <a class="nav-link" href="/home">หน้าหลัก</a>
    <a class="nav-link" href="/home">ค้นหาสัตว์</a>
    <a class="nav-link" href="/recommended">รายการแนะนำ</a>
    <a class="nav-link active" href="/favorites">รายการโปรด</a>

    <div class="user-badge ms-3">
      <span><%= (user && user.name) ? user.name : '' %></span>
      <img src="https://cdn-icons-png.flaticon.com/512/847/847969.png">
    </div>
  </div>
</nav>

<section class="hero" style="background-image: url('/banner.jpg'); background-size: cover; background-position: center;">
  <div class="hero-overlay"></div>
</section>

<div class="container-fluid mt-4">
  <div class="row">

    <div class="col-3">
      <div class="sidebar">
        <button class="menu-btn" onclick="location.href='/home'">ค้นหาสัตว์</button>
        <button class="menu-btn" onclick="location.href='/recommended'">รายการแนะนำ</button>
        <button class="menu-btn active">รายการโปรด</button>
      </div>
    </div>

    <div class="col-9">
      <!-- Title -->
      <h5 class="mb-4">รายการโปรด</h5>

      <!-- Pet Cards -->
      <div class="pet-cards-container">
        <% pets.forEach(pet => { %>
        <div class="pet-card favorite-item" data-petid="<%= pet.id %>" onclick="location.href='/pet/<%= pet.id %>'" style="cursor: pointer;">
          <img src="<%= pet.image %>" alt="<%= pet.name %>">
          <span class="heart-icon" onclick="event.stopPropagation(); toggleFavorite(this, '<%= pet.id %>')">❤️</span>
          <h6><%= pet.name %></h6>
          <small class="text-muted">รหัส: <%= pet.petId || '-' %></small>
          <p>
            <%= (pet.gender === 'female' || pet.gender === '♀') ? '♀' : '♂' %>
            <%= (pet.gender === 'female' || pet.gender === '♀') ? 'เพศเมีย' : 'เพศผู้' %>
            <%= pet.age %> 🎂
            <% if ((pet.vaccinated || '').toLowerCase() === 'yes') { %> 💉 <% } %>
          </p>
          <div class="pet-footer">
            <span class="badge-small" onclick="event.stopPropagation(); location.href='/pet/<%= pet.id %>'">รายละเอียด</span>
          </div>
        </div>
        <% }); %>
      </div>
      
      <div id="no-favorites" style="text-align: center; padding: 40px; color: #999; display: <%= pets.length ? 'none' : 'block' %>;">
        <p>ยังไม่มีรายการโปรด 💔</p>
      </div>
    </div>

  </div>
</div>

<footer class="text-center text-muted mt-5 mb-3">
  © 2026 Home4Pets | Project for Education
</footer>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
// Toggle favorite (remove from favorites)
async function toggleFavorite(element, petId) {
  const body = new URLSearchParams({ petId });
  const response = await fetch('/favorites/toggle', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body
  });
  const data = await response.json();
  if (data && data.success && !data.favorited) {
    const card = element.closest('.favorite-item');
    if (card) card.remove();
    const remaining = document.querySelectorAll('.favorite-item');
    if (remaining.length === 0) {
      document.getElementById('no-favorites').style.display = 'block';
    }
  }
}
</script>

</body>
</html>
