<!DOCTYPE html>
<!-- ===== Page: Login ===== -->
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>เข้าสู่ระบบ - Home4Pets</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" href="/style.css">
</head>
<body class="auth-page" style="background-color: #7fd1c2;">

<!-- Auth Container -->
<div class="auth-container">
  <div class="auth-card">
    <!-- Logo with House Icon -->
    <div class="auth-logo">
      <img src="/green_house_logo-removebg-preview.png" alt="Logo" style="height: 30px; vertical-align: middle; margin-right: 8px;"><span>Home4Pets</span>
    </div>

    <!-- Title -->
    <h3 class="auth-title">เข้าสู่ระบบ</h3>

   <!-- Error Message  -->
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <!-- Form -->
    <form method="POST" action="/login">
      <div class="form-group">
        <input type="email" name="email" class="auth-input" placeholder="📧 Email" required>
      </div>

      <div class="form-group password-field">
        <input type="password" id="passwordInput" name="password" class="auth-input" placeholder="🔒 Password" required>
        <button type="button" class="toggle-password" aria-label="แสดงรหัสผ่าน" aria-pressed="false">แสดง</button>
      </div>

      <a href="/forgot-password" class="forgot-password">ลืมรหัสผ่าน?</a>

      <button type="submit" class="auth-btn">เข้าสู่ระบบ/สมัครสมาชิก</button>
    </form>

    <!-- Sign Up Link -->
    <p class="auth-footer">
      ยังไม่มีสมาชิก <a href="/signup">สมัครสมาชิก</a>
    </p>
  </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
  const togglePasswordBtn = document.querySelector('.toggle-password');
  const passwordInput = document.getElementById('passwordInput');

  if (togglePasswordBtn && passwordInput) {
    togglePasswordBtn.addEventListener('click', () => {
      const isPassword = passwordInput.type === 'password';
      passwordInput.type = isPassword ? 'text' : 'password';
      togglePasswordBtn.textContent = isPassword ? 'ซ่อน' : 'แสดง';
      togglePasswordBtn.setAttribute('aria-label', isPassword ? 'ซ่อนรหัสผ่าน' : 'แสดงรหัสผ่าน');
      togglePasswordBtn.setAttribute('aria-pressed', String(isPassword));
    });
  }
</script>

</body>
</html>
