<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>รายการโปรด - Home4Pets</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<nav class="navbar">
  <div class="d-flex align-items-center gap-2">
    <img src="/green_house_logo-removebg-preview.png" alt="Logo" style="height: 30px; margin-right: 8px;"> <a href="/home" style="text-decoration: none;"><span class="navbar-brand">Home4Pets</span></a>
  </div>

  <div class="d-flex align-items-center">
    <a class="nav-link" href="/home">หน้าหลัก</a>
    <a class="nav-link" href="/home">ค้นหาสัตว์</a>
    <a class="nav-link" href="/recommended">รายการแนะนำ</a>
    <a class="nav-link active" href="/favorites">รายการโปรด</a>

    <div class="user-badge ms-3">
      <span>Kittisak</span>
      <img src="https://cdn-icons-png.flaticon.com/512/847/847969.png">
    </div>
  </div>
</nav>

<section class="hero" style="background-image: url('/banner.jpg'); background-size: cover; background-position: center;">
  <div class="hero-overlay"></div>
 >node app.jsnode app.js
  </div>
</section>

<div class="container-fluid mt-4">
  <div class="row">

    <div class="col-3">
      <div class="sidebar">
        <button class="menu-btn" onclick="location.href='/home'">ค้นหาสัตว์</button>
        <button class="menu-btn" onclick="location.href='/recommended'">รายการแนะนำ</button>
        <button class="menu-btn active">รายการโปรด</button>
      </div>
    </div>

    <div class="col-9">
      <!-- Title -->
      <h5 class="mb-4">รายการโปรด</h5>

      <!-- Pet Cards -->
      <div id="favorites-container" class="pet-cards-container">
        <!-- Favorites will be loaded here -->
      </div>
      
      <div id="no-favorites" style="text-align: center; padding: 40px; color: #999;">
        <p>ยังไม่มีรายการโปรด 💔</p>
      </div>
    </div>

  </div>
</div>

<footer class="text-center text-muted mt-5 mb-3">
  © 2026 Home4Pets | Project for Education
</footer>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
// Load favorites from localStorage
function loadFavorites() {
  const favorites = JSON.parse(localStorage.getItem('favorites') || '[]');
  const container = document.getElementById('favorites-container');
  const noFavoritesMsg = document.getElementById('no-favorites');
  
  if (favorites.length === 0) {
    container.style.display = 'none';
    noFavoritesMsg.style.display = 'block';
    return;
  }
  
  container.style.display = 'grid';
  noFavoritesMsg.style.display = 'none';
  container.innerHTML = '';
  
  favorites.forEach(pet => {
    const card = document.createElement('div');
    card.className = 'pet-card';
    card.style.background = 'linear-gradient(135deg, #ffc9e3 0%, #ffb3d9 100%)';
    card.innerHTML = `
      <img src="/แมว.jpg" alt="Pet">
      <h6>${pet.name}</h6>
      <p>${pet.gender} ${pet.age} 💉</p>
      <div class="pet-footer">
        <span class="heart-icon" onclick="removeFavorite(this, '${pet.id}')">❤️</span>
        <span class="badge-small">รับเลี้ยง</span>
      </div>
    `;
    container.appendChild(card);
  });
}

function removeFavorite(element, petId) {
  let favorites = JSON.parse(localStorage.getItem('favorites') || '[]');
  favorites = favorites.filter(pet => pet.id !== petId);
  localStorage.setItem('favorites', JSON.stringify(favorites));
  loadFavorites();
}

// Load on page load
window.addEventListener('load', loadFavorites);
</script>

</body>
</html>
