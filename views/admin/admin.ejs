<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>Admin Dashboard - Home4Pets</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<nav class="navbar">
  <div class="d-flex align-items-center gap-2">
    <img src="/green_house_logo-removebg-preview.png" alt="Logo" style="height: 30px; margin-right: 8px;"> <a href="/" style="text-decoration: none;"><span class="navbar-brand">Home4Pets</span></a>
  </div>

  <div class="d-flex align-items-center">
    <a class="nav-link" href="/">จัดการข้อมูลสัตว์เลี้ยง</a>
    <a class="nav-link" href="/">ค้นหาสัตว์</a>
    <a class="nav-link" href="/">ตั้งค่า</a>

    <div class="user-badge ms-3">
      <span><%= user.name %></span>
      <img src="https://cdn-icons-png.flaticon.com/512/847/847969.png">
    </div>
  </div>
</nav>

<!-- Dashboard Header -->
<div class="admin-header" style="background-image: url('/banner.png'); background-size: cover; background-position: center;">
  <h2>Dashboard</h2>
</div>

<div class="container-fluid">
  <div class="row">
    <!-- Sidebar -->
    <div class="col-3">
      <div class="admin-sidebar">
        <button class="admin-menu active" onclick="showTab('pets')">จัดการข้อมูลสัตว์เลี้ยง</button>
        <button class="admin-menu" onclick="showTab('add')">ค้นหาสัตว์</button>
        <button class="admin-menu" onclick="showTab('settings')">ตั้งค่า</button>
      </div>
    </div>

    <!-- Content -->
    <div class="col-9">
      <!-- Tab 1: Manage Pets -->
      <div id="petsTab" class="admin-tab">
        <h5 class="mb-4">รายชื่อสัตว์เลี้ยง <button class="btn btn-add" onclick="location.href='/admin/add-pet'">+ Add Pet</button></h5>
        
        <div class="pet-list">
          <% pets.forEach(pet => { %>
          <div class="pet-item">
            <img src="<%= pet.image %>" alt="Pet">
            <div class="pet-details">
              <h6><%= pet.name %></h6>
              <p><%= pet.type === 'cats' ? 'น้องแมว' : 'น้องสุนัข' %> รหัส: <%= pet.id %><br><%= pet.gender %> <%= pet.gender === '♀' ? 'เพศเมีย' : 'เพศผู้' %> อายุ <%= pet.age %></p>
              <div class="pet-status">
                <span class="status-health">✓ พร้อมรับเลี้ยง</span>
                <span class="status-toggle">
                  <input type="checkbox" checked>
                </span>
                <span class="status-toggle">
                  <input type="checkbox" checked>
                </span>
              </div>
            </div>
            <button class="btn btn-edit" onclick="location.href='/admin/edit-pet?id=<%= pet.id %>'">Edit</button>
          </div>
          <% }); %>
        </div>
      </div>

      <!-- Tab 2: Add Pet -->
      <div id="addTab" class="admin-tab" style="display: none;">
        <h5 class="mb-4">ค้นหาสัตว์</h5>
        
        <div class="add-form">
          <div class="form-row">
            <div class="form-group">
              <label>ชื่อ</label>
              <input type="text" placeholder="กรอกชื่อ" class="form-input">
            </div>
            <div class="form-group">
              <label>code</label>
              <input type="text" placeholder="กรอกรหัส" class="form-input">
            </div>
          </div>

          <div class="form-filters">
            <label>type</label>
            <select class="form-select">
              <option>-เลือก-</option>
              <option>Dogs</option>
              <option>Cats</option>
            </select>

            <label>Gender</label>
            <select class="form-select">
              <option>-เลือก-</option>
              <option>Male</option>
              <option>Female</option>
            </select>

            <label>Age</label>
            <select class="form-select">
              <option>-เลือก-</option>
              <option>0-1 years</option>
              <option>1-3 years</option>
              <option>3-5 years</option>
            </select>

            <label>Health</label>
            <select class="form-select">
              <option>-เลือก-</option>
              <option>Healthy</option>
              <option>Problem</option>
            </select>

            <label>Color</label>
            <select class="form-select">
              <option>-เลือก-</option>
              <option>White</option>
              <option>Black</option>
              <option>Brown</option>
            </select>

            <label>Status</label>
            <select class="form-select">
              <option>-เลือก-</option>
              <option>Available</option>
              <option>Adopted</option>
            </select>

            <button class="btn btn-search">🔍 ค้นหา</button>
          </div>
        </div>
      </div>

      <!-- Tab 3: Settings -->
      <div id="settingsTab" class="admin-tab" style="display: none;">
        <h5 class="mb-4">ตั้งค่า</h5>
        
        <div class="settings-content">
          <p>เปลี่ยนรหัสผ่าน (Change Password)</p>
          <p>ประวัติการเข้าใจงาน (Login Activity)</p>
          <a href="/logout" class="btn btn-danger" style="display: inline-block; margin-top: 20px;">ออกจากระบบ (Logout)</a>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
function showTab(tabName) {
  // Hide all tabs
  document.getElementById('petsTab').style.display = 'none';
  document.getElementById('addTab').style.display = 'none';
  document.getElementById('settingsTab').style.display = 'none';

  // Remove active class from all buttons
  var buttons = document.querySelectorAll('.admin-menu');
  buttons.forEach(function(btn) {
    btn.classList.remove('active');
  });

  // Show selected tab and mark button as active
  if (tabName === 'pets') {
    document.getElementById('petsTab').style.display = 'block';
    buttons[0].classList.add('active');
  } else if (tabName === 'add') {
    document.getElementById('addTab').style.display = 'block';
    buttons[1].classList.add('active');
  } else if (tabName === 'settings') {
    document.getElementById('settingsTab').style.display = 'block';
    buttons[2].classList.add('active');
  }
}
</script>

</body>
</html>
